export class Head {
    public right
    public left
    public up
    public down

    constructor(r: number, l: number, u: number, d: number) {
        this.right = r
        this.left = l
        this.up = u
        this.down = d
    }
}