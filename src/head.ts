export class Head {
    public still
    public right
    public left
    public up
    public down

    constructor(s: number, r: number, l: number, u: number, d: number) {
        this.still = s
        this.right = r
        this.left = l
        this.up = u
        this.down = d
    }
}